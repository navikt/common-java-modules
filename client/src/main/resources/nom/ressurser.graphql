query($identer: [String!]!) {
    ressurser(where: { navIdenter: $identer }){
        id
        ressurs {
            navIdent
            visningsnavn
            visningsfornavn
            visningsetternavn
        }
    }
}