query($identer: [String!]!) {
    ressurs(where: { navIdenter: $identer }){
        navIdent
        person {
            navn {
                fornavn
                mellomnavn
                etternavn
            }
        }
    }
}