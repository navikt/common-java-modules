package no.nav.brukerdialog.security.oidc.provider;

public class InternalUserLoginProvider extends AzureADB2CProvider{
    public InternalUserLoginProvider(AzureADB2CConfig config) {
        super(config);
    }
}
